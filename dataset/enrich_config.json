{
  "model": "gpt-5.6-luna",
  "reasoning_effort": "low",
  "temperature": null,
  "note": "Enrichment settings for dataset/enrich.py. gpt-5.6-luna with reasoning effort 'low' is the fast/cheap tier suited to extraction; structured outputs guarantee schema-valid responses; temperature null = parameter omitted (reasoning models require the default)."
}
